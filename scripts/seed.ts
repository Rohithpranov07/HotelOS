// Placeholder seed script — full implementation lands in T-02.
// Run: pnpm seed
async function main() {
  // eslint-disable-next-line no-console
  console.log('Seed placeholder — Prisma client + schema arrive in T-02.');
}

main().catch((err) => {
  // eslint-disable-next-line no-console
  console.error(err);
  process.exit(1);
});
